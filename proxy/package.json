{
  "name": "roboplc-proxy",
  "version": "1.0.0",
  "description": "Fastify proxy server for RoboPLC middleware",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rimraf dist"
  },
  "keywords": ["roboplc", "proxy", "fastify"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
